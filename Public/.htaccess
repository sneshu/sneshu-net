RewriteEngine On

# Force https
RewriteCond %{HTTP_HOST} !^localhost
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://sneshu.net/$1 [R=301,L]

RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

RewriteRule ^ index.html [L]
